@page
@model GameWebApp.Pages.GameRooms.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GameRoom[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GameRoom[0].PlayerNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.GameRoom) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlayerNumber)
            </td>
            <td>
                <form  mothod="post">
                    <button asp-page-handler="JoinGameRoom" onclick="return confirm('Are you sure')">Join</button>
                </form>
                <!--<a asp-page="../Players/Create" asp-route-id="@item.ID">Join</a> |-->
                <!--<a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |-->
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <!--<a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>-->
            </td>
        </tr>
}
    </tbody>
</table>
